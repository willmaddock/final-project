<div id="<%= dom_id access_log %>">
  <p>
    <strong>User:</strong>
    <%= access_log.user.username %> <!-- Assuming user has a username attribute -->
  </p>

  <p>
    <strong>Access Point:</strong>
    <%= access_log.access_point.location %> <!-- Using location instead of name -->
  </p>

  <p>
    <strong>Timestamp:</strong>
    <%= access_log.timestamp.strftime("%B %d, %Y at %I:%M %p") if access_log.timestamp %>
  </p>

  <p>
    <strong>Successful:</strong>
    <%= access_log.successful ? 'Yes' : 'No' %>
  </p>
</div>